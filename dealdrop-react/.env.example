# Copy to `.env.local` and adjust. Vite only exposes vars prefixed with VITE_.

# Set to `true` to show 8 sample Chennai-area deals on the home page (for demo video).
# Use `false` or remove for normal / production behaviour.
VITE_DEMO_SEED=false
